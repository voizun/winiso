name: Win11ISO

on:
  workflow_dispatch:

jobs:
  Win11ISO:
    runs-on: windows-2025
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: unzip *.zip

      - run: |
          sed -i -e "s/powershell/pwsh/" uup_download_windows.cmd
          sed -i -e 's/$/\r/g' uup_download_windows.cmd
          cd Drivers
          curl -L -O GPD-All-WIN10_X64_drivers_V6.tar.zst "https://limewire.com/decrypt/download?downloadId=c6f88656-e0aa-4a5a-b24d-608d19685ba7"
          tzr xf GPD-All-WIN10_X64_drivers_V6.tar.zst
          tree
          
      - run: .\uup_download_windows.cmd

      - run: split -b 2000M -d *.ISO WIN11.ISO
        shell: bash


      - run: gh release create Win11ISO WIN11.ISO??
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
      
