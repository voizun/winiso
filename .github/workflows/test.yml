name: Win11ISO

on:
  workflow_dispatch:

jobs:
  Win11ISO:
    runs-on: windows-2025
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: unzip *.zip

      - run: |
          sed -i -e "s/powershell/pwsh/" uup_download_windows.cmd
          sed -i -e 's/$/\r/g' uup_download_windows.cmd
          cd Drivers
          curl -L -O GPD-All-WIN10_X64_drivers_V6.zip "https://drive.usercontent.google.com/download?id=1bH1ysDkHJzTL98QR5X6XSvTaZeV-mZMC&export=download&authuser=1&confirm=t&uuid=2036685c-08a1-463d-895c-b5d192711e2d&at=ALWLOp4Ja8dzInfFiG4cxpdUXSbY%3A1764808309795"
          unzip *.zip
          tree
          
      - run: .\uup_download_windows.cmd

      - run: split -b 2000M -d *.ISO WIN11.ISO
        shell: bash


      - run: gh release create Win11ISO WIN11.ISO??
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
      
